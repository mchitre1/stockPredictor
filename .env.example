# Optional: Finnhub API key for news (get free key at https://finnhub.io)
# Without it, predictions use only price data.
FINNHUB_API_KEY=

# Flask
FLASK_ENV=development
SECRET_KEY=change-me-in-production
